<%- include('partials/header') %>

<% articles.forEach(article => { %>
<div class="card">
    <div class="card-header">
        <%= article.title %>
    </div>
    <div class="card-body">
        <%= article.body %>
    </div>
</div>
<% }) %>

<div id="comments">

</div>

<%- include('partials/footer') %>